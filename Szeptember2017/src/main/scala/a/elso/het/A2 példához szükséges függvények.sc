
val rendezve = List(28, 1, 19, 3).sorted

val megfordítva = List(1, 19, 3).reverse

val elemekSzáma = List(1, 2, 3, 4).size

val összege = List(10, 40, 50).sum

val különbség = List(1, 2, 3, 4, 5).diff(List(2, 3, 4))
